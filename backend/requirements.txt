# Phase 1 — parsing
tree-sitter==0.26.0
tree-sitter-python==0.25.0

# Phase 2 — graph engine
networkx>=3.0

# Phase 3 — AI / retrieval
# llm_client.py imports these lazily, so install only the provider(s)
# you'll actually use for the demo. LLM_PROVIDER env var selects one;
# defaults to "mock" (no key, no network — safe for dev/CI).
# anthropic>=0.40
# openai>=1.50
google-genai>=1.0   # current SDK — NOT google-generativeai, which Google retired Nov 30, 2025

# Phase 5 — backend integration + test-gen pre-bake
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.0
pytest>=8.0
pytest-cov>=5.0
coverage>=7.0
httpx>=0.27
